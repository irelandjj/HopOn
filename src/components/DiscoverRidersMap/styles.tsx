import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    map: {
        minHeight: '50%',
        maxHeight: '100%', 
        width: '100%',
    }
});

export default styles;