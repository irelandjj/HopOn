import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    backIcon: {
        position: 'absolute',
        top: 5,
        left: 5,
        zIndex: 100,
    },
});

export default styles;